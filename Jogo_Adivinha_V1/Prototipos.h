#pragma once

typedef struct
{
    char* questoes[100];
    char opc[3][50];
    char respostas;

} Perguntas;

//3 Struct de 3 Categorias diferentes.

void dificuldade(char a, int* b);
void Perguntas(int* b);